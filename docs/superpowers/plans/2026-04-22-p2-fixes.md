# P2 Fixes — All Remaining Low-Severity Issues

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all 26 P2 issues from the UI/UX audit report.

**Architecture:** Each task is independent. Grouped into batches by module affinity.

**Tech Stack:** Swift / SwiftUI / iOS 17+ / `@Observable`

---

## Batch A — Quick Targeted Fixes (12 tasks)

### Task 1: Unify toast background styles
Module 1 — LoginView toast uses `Theme.textDeep.opacity(0.92)` (dark gray) while PhoneVerificationView uses `Theme.primary.opacity(0.92)` (green). Inconsistent across auth pages.

**Files:** `LoginView.swift`, `PhoneVerificationView.swift`

- [ ] Find all toast background colors in auth views
- [ ] Unify to a single style — use `Theme.textDeep.opacity(0.92)` (dark capsule) as the standard informational toast, keep `Theme.primary` only for success-specific toasts
- [ ] Commit

### Task 2: Fix follow button style inconsistency
Module 2 — HomeView recommended player cards: unfollowed=green solid, followed=transparent border. MatchDetailView: reversed. Should be consistent.

**Files:** `MatchDetailView.swift`

- [ ] Read HomeView's recommendation card follow button style (the "standard")
- [ ] Update MatchDetailView's follow button to match: unfollowed=green solid, followed=transparent border
- [ ] Commit

### Task 3: Fix chat date label hardcoded "今天"
Module 4 — `ChatDetailView.swift:111` — date separator always says "今天" regardless of actual date.

**Files:** `ChatDetailView.swift`

- [ ] Replace hardcoded "今天" with a computed value using `Calendar.current.isDateInToday(Date())` check — for mock purposes, always returns "今天" but the code structure is correct for when real dates exist
- [ ] Actually, since all mock messages are current-session, keep "今天" but wrap in a helper function `dateSeparatorLabel(for date: Date) -> String` that returns "今天"/"昨天"/formatted date. Use Date() for now.
- [ ] Commit

### Task 4: Fix invitation card accept button touch target
Module 4 — `ChatDetailView.swift:490` — "接受" button is 26pt height, below HIG 44pt minimum.

**Files:** `ChatDetailView.swift`

- [ ] Find the invitation accept button
- [ ] Add `.frame(minHeight: 44)` to ensure touch target compliance while keeping visual size
- [ ] Commit

### Task 5: Fix ProfileView hardcoded stats
Module 5 — `ProfileView.swift:261, 321-322` — "出席率" "總場次" "本月場次" hardcoded as "92%", "28", "5".

**Files:** `ProfileView.swift`

- [ ] Add computed properties or UserStore fields for attendance rate, total matches, monthly matches
- [ ] For mock stage, derive from mock data or UserStore with reasonable defaults
- [ ] Commit

### Task 6: Fix "理想球友" label always showing
Module 5 — `ProfileView.swift:188-194` — Gold "理想球友" badge always shows regardless of user status.

**Files:** `ProfileView.swift`

- [ ] Add a condition: only show badge if user has high reputation (e.g., reputation >= 90) or some qualifying criteria
- [ ] For mock stage, compute from UserStore data
- [ ] Commit

### Task 7: Add success feedback to CreateTournamentView
Module 6 — `CreateTournamentView.swift:625-628` — Direct dismiss after publish, no toast or success indication.

**Files:** `CreateTournamentView.swift`

- [ ] Add `@State private var publishToast: String?`
- [ ] On successful publish, show "賽事已成功發布" toast before dismissing (with delay)
- [ ] Add toast overlay
- [ ] Commit

### Task 8: Fix MatchAssistantView endHour overflow
Module 6 — `MatchAssistantView.swift:185, 195` — When startHour=23, endHour=25, displays "23:00 - 25:00".

**Files:** `MatchAssistantView.swift`

- [ ] Clamp endHour to max 24 (or use `min(endHour, 24)`)
- [ ] If endHour would exceed 24, cap display at "23:59" or "00:00(隔天)"
- [ ] Commit

### Task 9: Extend ChangePasswordSheet toast duration
Module 7 — `SettingsView.swift:266-268` — Success toast auto-dismisses in 1.2s, too fast to read.

**Files:** `SettingsView.swift`

- [ ] Change the `asyncAfter` delay from 1.2 to 2.0 seconds
- [ ] Commit

### Task 10: Fix ReviewsView default 5-star rating
Module 7 — `ReviewsView.swift:15` — Rating defaults to 5 stars, users can submit without adjusting.

**Files:** `ReviewsView.swift`

- [ ] Change default rating from 5 to 0
- [ ] Disable submit button when rating == 0
- [ ] Show hint text like "請先選擇評分" when rating is 0
- [ ] Commit

### Task 11: Add CreateTournamentView unsaved changes warning
Module 6 — Filling many fields then tapping back discards all without warning.

**Files:** `CreateTournamentView.swift`

- [ ] Add `hasUnsavedChanges` computed property checking if any field is non-default
- [ ] Add `@State private var showDiscardAlert = false`
- [ ] Override back button to check unsaved changes
- [ ] Add discard confirmation alert
- [ ] Commit

### Task 12: Fix tournament button touch targets
Module 6 — `TournamentView.swift:108, 225` — "建立賽事" button 28pt, signup button 26pt height.

**Files:** `TournamentView.swift`

- [ ] Find buttons with heights < 44pt
- [ ] Add `.frame(minHeight: 44)` or increase `.frame(height:)` to 44
- [ ] Commit

---

## Batch B — Chat & Messages Improvements (4 tasks)

### Task 13: Add mock chat messages
Module 4 — `ChatDetailView.swift:633` / `MessagesView.swift:270` — Empty mock data means chat tab always shows empty state.

**Files:** `ChatDetailView.swift`, `MessagesView.swift`

- [ ] Add 5-8 mock messages to `mockMessages` array (mix of incoming/outgoing/system messages)
- [ ] Ensure `chats` in MessagesView/HomeView has non-empty mock data (verify `mockChatsInitial`)
- [ ] Commit

### Task 14: Fix chat card vs row style inconsistency
Module 4 — `MessagesView.swift:37-66` — First chat uses card style, rest use row style, no functional difference.

**Files:** `MessagesView.swift`

- [ ] Unify all chat items to use the same row style
- [ ] Remove the special card treatment for the first item
- [ ] Commit

### Task 15: Add photo preview before sending
Module 4 — `ChatDetailView.swift:566-579` — Selected photo is sent immediately without preview.

**Files:** `ChatDetailView.swift`

- [ ] Add `@State private var pendingPhotoData: Data?`
- [ ] On photo selection, set `pendingPhotoData` instead of immediately appending to `sentMessages`
- [ ] Show a preview sheet/overlay with "發送" and "取消" buttons
- [ ] On confirm, append to sentMessages; on cancel, clear pendingPhotoData
- [ ] Commit

### Task 16: Fix invitation accept button height
(Same as Task 4 — merged. Skip if already done in Task 4.)

---

## Batch C — Data & Logic Fixes (5 tasks)

### Task 17: Fix LoginView navigation destinations count
Module 1 — LoginView has 5 `navigationDestination(isPresented:)` which can cause nav stack issues.

**Files:** `LoginView.swift`

- [ ] Read LoginView and count navigationDestinations
- [ ] Consolidate using `navigationDestination(item:)` with an enum if possible
- [ ] Or use a single NavigationPath approach
- [ ] Commit

### Task 18: Fix cancel dialog text too long
Module 3 — Cancel confirmation dialog has 7 lines of rules in the alert message.

**Files:** `MyMatchesView.swift`

- [ ] Find the cancel confirmation dialog
- [ ] Shorten the message to 2-3 key lines, remove detailed rules
- [ ] Commit

### Task 19: Fix NotificationsView hardcoded MatchDetailData
Module 7 — `NotificationsView.swift:128-195` — All notification-generated match details use fixed dates/locations.

**Files:** `NotificationsView.swift`

- [ ] Update notification-generated MatchDetailData to derive from the notification content
- [ ] Use the notification's own text to populate date, location, etc.
- [ ] Commit

### Task 20: Fix MatchAssistantView endHour (merged with Task 8)

### Task 21: Add PublicProfileView DM navigation
Module 5 — `PublicProfileView.swift:281-288` — DM button creates MockChat but has no navigationDestination.

**Files:** `PublicProfileView.swift`

- [ ] Add `@State private var navigateToChat = false` and appropriate chat state
- [ ] Add `.navigationDestination(isPresented: $navigateToChat)` to ChatDetailView
- [ ] Commit

---

## Batch D — Refactoring & Code Quality (5 tasks)

### Task 22: Replace Foundation Timer with SwiftUI timer
Module 1 — `PhoneVerificationView.swift:227` and `EmailRegisterView.swift:258` use `Timer.scheduledTimer`.

**Files:** `PhoneVerificationView.swift`, `EmailRegisterView.swift`

- [ ] Replace `Timer.scheduledTimer` with `.onReceive(Timer.publish(every: 1, on: .main, in: .common).autoconnect())`
- [ ] Remove timer invalidation code, use @State flag to control
- [ ] Commit

### Task 23: Extract shared NTRP dual-slider component
Module 3 — HomeView and CreateMatchView have nearly identical dual-slider implementations.

**Files:** Create `NTRPRangeSlider.swift`, modify `HomeView.swift`, `CreateMatchView.swift`, `EditProfileView.swift`

- [ ] Extract the NTRP dual-thumb slider into a shared `NTRPRangeSlider` view
- [ ] Parameters: `lowValue: Binding<Double>`, `highValue: Binding<Double>`, `range: ClosedRange<Double>`
- [ ] Replace inline implementations in HomeView, CreateMatchView, EditProfileView
- [ ] Commit

### Task 24: Add AchievementsView badge detail interaction
Module 7 — Clicking badges does nothing, no progress display.

**Files:** `AchievementsView.swift`

- [ ] Add `@State private var selectedBadge: Achievement?`
- [ ] Add a detail sheet showing badge description, unlock date, progress
- [ ] Commit

### Task 25: Shorten invitation decline persistence
Module 3 — `@AppStorage` stores JSON-encoded declined invitation IDs, has size limits.

**Files:** Review current implementation, assess if a change is needed

- [ ] Check current implementation size
- [ ] If the list is small (< 50 items), the current approach is fine for mock stage — add a comment noting the limitation
- [ ] If needed, trim old entries or switch to a more robust storage
- [ ] Commit

### Task 26: Extract shared follow list row component
Module 5 — FollowingView, FollowerListView, MutualFollowListView have ~90% duplicate playerRow code.

**Files:** Create shared component, modify 3 list views

- [ ] Extract a shared `FollowPlayerRow` view
- [ ] Parameters: player, isMutual, follow action, tap action
- [ ] Replace inline playerRow functions in all 3 views
- [ ] Commit
